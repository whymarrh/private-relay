{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc --project .",
    "test": "yarn build && node --harmony-top-level-await index.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.5",
    "baretest": "^2.0.0",
    "node-fetch": "^3.1.0",
    "typescript": "^4.5.2"
  }
}
