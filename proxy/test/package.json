{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project .",
    "test": "yarn build && node --harmony-top-level-await index.js"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "baretest": "^2.0.0",
    "node-fetch": "^2.6.1",
    "typescript": "^4.0.3"
  }
}
