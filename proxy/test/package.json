{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc --project .",
    "test": "yarn build && node --harmony-top-level-await index.js"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "baretest": "^2.0.0",
    "node-fetch": "^2.6.1",
    "typescript": "^4.2.2"
  }
}
