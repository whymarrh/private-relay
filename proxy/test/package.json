{
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "build": "tsc --project .",
    "test": "yarn build && node --harmony-top-level-await index.js"
  },
  "devDependencies": {
    "@types/node": "^18.11.10",
    "baretest": "^2.0.0",
    "node-fetch": "^3.2.3",
    "typescript": "^4.6.3"
  }
}
